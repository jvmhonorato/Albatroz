export type FormType = {
  email: string;
  password?: string;
};

export type TParams = {
  name: string;
  url: string;
};

export type ResetPasswordForm = {
  password: string;
  confirmPassword: string;
};
